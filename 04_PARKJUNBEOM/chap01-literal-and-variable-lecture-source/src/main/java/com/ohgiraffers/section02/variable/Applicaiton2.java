package com.ohgiraffers.section02.variable;

public class Applicaiton2 {
    public static void main(String[] args) {

        // 1. 정수를 취급하는 자료형
        // 변수를

        // 2. 실수를 취급하는 자료형

        // 3. 문자를 취급하는 자료형

        // 4. 논리값을 취급하는 자료형
        boolean isTrue;

        // 5. 문자열을 취급하는 자료형(참조 자료형)

        // 초기화 작업 (자료형 별로 초기화)
        // long형에 대입되는 값은 L을 붙인다.
        // double형으로 인식되는 실수를 float에 담을 때는 f를 붙인다.

        // char는 함수만 가능하다.

        // double avg = sum / 3.0; // 나눗셈은 일반적으로 실수 결과가 나오며 나눌 때


    }
}
